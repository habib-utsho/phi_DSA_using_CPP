#include <bits/stdc++.h>;
using namespace std;

int main()
{
    vector<int> v = {5, 10, 15};

    for (auto it = v.begin(); it < v.end(); it++)
    {
        cout << *it << " " ;
    }

    return 0;
}